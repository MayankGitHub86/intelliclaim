server:
  port: 8082

spring:
  application:
    name: claim-service
  datasource:
    url: jdbc:postgresql://localhost:5432/intelliclaim_claims
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
